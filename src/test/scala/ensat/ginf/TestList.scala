package ensat.ginf

import ensat.ginf.basics.Parser;

class TestList /*extends FlatSpec*/ {
  /*"This one " should
  "return a list with that file name" in {
    val parser=Parser
    parser.parseList("DATA.txt")
     assert(parser.parseList("DATA.txt") == 1)
  }*/
}